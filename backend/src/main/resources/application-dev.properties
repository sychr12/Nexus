# Local development defaults. Use SPRING_PROFILES_ACTIVE=prod in production.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/sicpr}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:120506}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update

jwt.secret=${JWT_SECRET:dev_sicpr_jwt_secret_change_me_with_at_least_32_chars}
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
app.crypto.key=${DATA_ENCRYPTION_KEY:dev_sicpr_crypto_key_change_me_32_chars}
