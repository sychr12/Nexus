export default function AdicionarPage() {
  /*
    Tela Adicionar pausada temporariamente.

    A rota foi mantida para facilitar reativacao futura, mas a aba foi retirada
    da Sidebar para nao aparecer ao usuario enquanto estiver fora de uso.

    Implementacao anterior:

    import { redirect } from "next/navigation";

    export default function AdicionarPage() {
      redirect("/dashboard");
    }
  */
  return null;
}
